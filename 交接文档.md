# 绿角犀看图 · 项目交接总结

> 给新接手者的第一份文档。**先读本文件**，再按需查 `CHANGELOG.md` / `overview.md` / `BUILD.md`。
> 交接日期：2026-09-04；最近刷新：2026-09-10（瘦身瘦脸迭代）。
> ⚠️ 本机 AI 记忆（`project_memory.md` / `user_profile.md`）位于 `C:\Users\Administrator\.trae-cn\memory\`，**换机不会带过去**；关键硬约束与教训已下沉到本文件第 6/8 节，务必以此为准。

---

## 0. 三句话速览

- **是什么**：绿角犀看图 = 看图工具（图片浏览器 + 图片处理编辑器），以 **Web 原型**（PWA）为核心，用 **Tauri v2** 包装为 Windows 桌面应用；鸿蒙原生版 `harmony/` 为并行主线（代码就绪，待真机验证）。
- **技术栈**：前端原生 JS 单文件（`app.js` ~232KB，无框架）+ `index.html` + `styles.css`；后端 Rust（`src-tauri/src/lib.rs`）；构建出 NSIS + MSI（Setup.exe 已内嵌 WebView2 离线运行时）。
- **当前状态**：**桌面版可交付**，最新功能（美图左工具右属性 / 一键美颜三档 / 风格配方 25 预设 / AI 离线超分 / 一键抠图 / 批量压缩·转换·尺寸·滤镜实时预览 / 画面区域截图 / HEIC 离线解码 / **美图瘦身瘦脸局部液化**）已构建安装验收；前端回归 **224 项全绿**。待办：鸿蒙真机、功能体验细化。

---

## 1. 文档索引（遇到问题先查这）

| 文档 | 内容 | 何时查 |
|---|---|---|
| `交接文档.md`（本文件） | 项目速览 / 命令 / 结构 / 坑 | 新接手第一站 |
| `CHANGELOG.md` | 更新日志（9/9 → 9/10，倒序） | 了解已做了什么 |
| `overview.md` | 53KB 项目全档（PRD 实现细节） | 功能逻辑 |
| `BUILD.md` | 构建指南 + 踩坑实录 | 构建/打包报错 |
| `使用说明.md` / `.html` | 用户操作说明（应用内 ? 同理） | 用户操作 |
| `test/` 对应 .cjs | 测试用例即行为规约 | 测试失败定位 |
| `.trae-cn/memory/projects/.../project_memory.md` | 历次硬约束与教训（本机 AI 记忆） | 承接历史决策 |

---

## 2. 环境（构建机已具备）

- Rust 1.97.1 stable（x86_64-pc-windows-msvc）｜VS Build Tools（`C:\BuildTools`）｜Node ≥18（本机 22）
- WebView2 Evergreen 已装；Tauri 打包器缓存 `%LOCALAPPDATA%\tauri\`（NSIS 3.11 + WiX 已配好）
- 鸿蒙工具链**未装**（须 DevEco Studio 5.0+）

## 3. 常用命令

```bat
:: 测试（改代码后必跑；jsdom 在 .workbuddy 下的 workspace node_modules）
npm test                 :: regression 224 项
npm run test:photo       :: 照片处理 29 项
npm run test:crop        :: 裁剪 14 项
npm run test:recent      :: 最近打开 25 项
npm run test:beauty      :: 美图 52 项（含风格网格 + 批量预览断言）
node test/tone-smoke.cjs :: 色调引擎 17 项（纯 node）
node test/check-engine.cjs :: 关键函数完整性校验（防 Edit 未落盘）

:: PowerShell 直跑（npm scripts 的 NODE_PATH 语法在 PS 下会失效）
$env:NODE_PATH="C:/Users/Administrator/.workbuddy/binaries/node/workspace/node_modules"
node test/regression.cjs

:: Web 预览 / 桌面构建
node serve.js                :: 8080 Web 原型预览
npx tauri build              :: ⚠️ 先分别跑 cargo clean（全量）+ npx tauri build（见第 8 节坑#1）
build-windows.bat            :: 一键构建（含环境检查）
```

## 4. 目录结构

```
看图工具/
├── index.html / app.js / styles.css   # Web 前端主体（app.js 为全部前端逻辑，约 232KB）
├── dist/                              # 构建输入（scripts/sync-dist.cjs 从根目录同步，勿手改）
├── scripts/
│   ├── sync-dist.cjs                  # 构建前同步（tauri.conf beforeBuildCommand 自动调）
│   ├── cap_app.ps1                    # 桌面窗口 PrintWindow 捕获（被遮挡也能截）
│   ├── verify_edit.ps1                # UIA 后台点 🎨 打开编辑面板 + 截图
│   ├── verify_panel.ps1               # 参数化 beauty|batch 面板验收（真实鼠标点击 + UIA 按钮名判据）
│   ├── verify_compress.ps1            # 批量压缩页签验收
│   ├── verify_convert_resize.ps1      # 批量转换/尺寸页签验收
│   ├── verify_matting.ps1             # 一键抠图验收
│   └── gen-icons.cjs
├── src-tauri/
│   ├── src/lib.rs                     # 后端：load_paths/壁纸/剪贴板/托盘/单实例 + 稳定性参数注释
│   ├── tauri.conf.json                # beforeBuildCommand + additionalBrowserArgs（勿删 4 flags）
│   ├── installer.nsh                  # 右键菜单注册（图片/文件夹）
│   └── Cargo.toml
├── harmony/                           # 鸿蒙原生版（index.ets 约 300 行，待 DevEco 真机）
├── server/                            # 云端 mock 后端（契约测试）
├── test/                              # 全部测试（*.cjs）
├── CHANGELOG.md / BUILD.md / overview.md / 使用说明.md
└── scripts/ 及根目录大量兼容旧脚本（cap*/tray*/wp*/cdp* 等）——迭代遗留，功能已并入 scripts/ 两个新脚本
```

## 5. 近期关键变更（完整见 CHANGELOG.md）

- **9/4 修复启动不显示照片**：含空格/括号路径（如 `福娃设计 (3).png`）在某启动方式下会被 argv 拆成两段→收集 0 张→空界面；后端新增 `rejoin_existing` 重接 + 单实例转发唤起窗口并兜底 stash，前端 `get_pending_paths` 多次重试拉取，`cargo test` 3 项全过
- **9/4 单图开相邻**：打开单张图片自动加载同文件夹（同目录、非递归）相邻图到**左侧纵向缩略图栏**并可上下切换；前端定位到所打开那张；新增 Rust 单测 `collect_flat_lists_siblings_of_single_image`
- **9/4 UI 布局**：**缩略图栏由底栏移到左侧**（`.app-row` 弹性行，左缩略图 + 右主画布双栏，86px 纵向栏）；搜索条移入顶栏工具栏中组；滤镜参数两列排列（`.field-grid`，12 滑杆 2 列）
- **9/4 美图滤镜增强**：14 个美图公开配方（玄青ME8/杂志人像MN7/秋日电影VM5 等）+ 5 个高级滑杆（高光/暗部/褪色/颗粒/暗角）+ 色调分离（双取色器 + 强度）；纯 JS 像素质点引擎 `toneRows`（预览与导出共用，导出分块防卡）
- **9/4 配色规范**：青绿炭墨（主 #2C9678 / 辅 #373834 / 浅 #F5F4F7 / 深 #1C1C1C），UI/海报/插画严禁他色
- **9/4 最近打开**：空状态首页 6 缩略图横排（后端 `first_thumb`）
- **9/3 修复**：图片居中（transform-origin 50% 50%）、空状态残留遮挡、WebView2 随机退出（4 个浏览器参数）
- **9/2 验收**：Windows 安装包真机回归全过（关联/右键菜单/托盘/壁纸）
- **9/4→9/10 后续迭代（速览，详见 CHANGELOG）**：
  - **HEIC/HEIF 离线解码**：heic2any → libheif-wasm 本地化（`dist/libheif-bundle.js` ~1.9MB，内嵌 de265），彻底离线、真机 Chromium 端到端通过
  - **AI 离线放大**：内置 `sub_pixel_cnn.onnx`（~0.24MB）+ onnxruntime-web 本地化，YCbCr 瓦片 3× 推理，失败自动降级 Lanczos；`loadAiSession` 支持自定义模型 URL
  - **一键抠图（离线去背）**：≤512px 高斯似然比逐像素 alpha + 彩色图引导双边羽化，导出透明 PNG
  - **美图落地重构**：左工具分类+右属性面板（替代弹窗）嵌入主界面，常驻美图常规条，一键美颜三档，撤销/重做 + Ctrl+Z/Y
  - **批量面板实时预览**：压缩/转换/调整尺寸/滤镜四页签顶部均加所见即所得缩略预览 + 体积估算
  - **画面区域截图**：`snapVisible` 用 DOMMatrix 复刻位移/旋转/翻转/缩放/EXIF 框选导出 PNG
  - **美图瘦身/瘦脸（局部液化）**：高级面板分区，`state.slim` + `slimWarp/slimDisp/bilinear` 位移场；`slimCanvas` 导出 / `applySlimInPlace` 预览共用同算法；锚点拖拽定位 + 撤销/重做快照

## 6. 架构与关键实现

- **导出管线顺序**（exportCanvasOfCurrent，async）：旋转/EXIF/滤镜/色温/模糊 → 锐化卷积 → 裁剪 → OpenCV/AI 队列；单项失败跳过不阻塞
- **色调引擎**（app.js）：`STYLE_PRESETS`（25 格，含 none）定义全部风格；`toneRows` 掩码式处理（高光 `smstep(0.42,1,lum)`、阴影 `smstep(0.5,0,lum)`、颗粒 `gnash` 确定性散列、径向暗角）；预览同步跑小画布、导出 `applyToneChunked` 分块
- **桌面集成**：桌面加载在 `window.__TAURI__` 分支（`desktop.loadPaths` 等）；命令行/右键/拖拽/最近打开 → `load_paths` → PENDING_PATHS + `get_pending_paths` 事件
- **构建嵌入链路**：dist/（同步源）→ 构建时 sync-dist.cjs 刷新 → Rust 内嵌（**压缩**，exe 字节搜索已失效）

## 7. 桌面实机验证方法（重要，替代前端截屏）

前台截屏被系统前台锁定 + 用户其他窗口遮挡不可靠。**可靠链路**：
1. `scripts/cap_app.ps1 <pid> <out.png>`：EnumWindows 找窗口 → **PrintWindow(hwnd,hdc,2)** 直渲窗口内容（应用禁用 GPU 合成=CPU 渲染，PrintWindow 有效）→ 存 png
2. `scripts/verify_edit.ps1 <pid> <out.png>`：**UIA 后台 Invoke** 按钮（Chromium 无障碍树惰性初始化，需重试 2-3 次；「🎨」= U+1F3A8 surrogate D83C DFA8，btn[10]）打开编辑面板 → PrintWindow
3. keybd_event / 滚轮消息对**后台** WebView2 无效（Chromium 忽略非前台输入）；`$Pid` 是 PS 只读变量勿作参数名
4. 应用进程会被外部频繁关闭（无崩溃事件）——**验证务必合并为单脚本**：杀进程→启动带图→等 3s→UIA+截图→杀进程

## 8. 已知坑与规避（必读）

1. **前端重嵌**：`npx tauri build` 若 Rust 未变可能不重嵌前端（exe 带旧 UI）。**唯一可靠**：`cargo clean`（全量）→ `npx tauri build`。touch lib.rs 已证实不可靠。验证：看 dist/ mtime 晚于 sync 且 rg 新标记串在 dist/，再实机截图
2. **Edit 工具落盘风险**：编辑 app.js/.ps1 可能"报告成功但未落盘"。**改完必须 rg/Read 校验磁盘**，缺失即重编
3. **PowerShell 非 ASCII 编码**：PS 脚本须纯 ASCII（UTF-8 无 BOM 会被当 GBK 读而语句损坏）；中文匹配改用 surrogate 判断（如 `$n[0] -eq [char]0xD83C -and $n[1] -eq [char]0xDFA8`）
4. **PWA SW 缓存**：改前端后升级 `sw.js` 的 `CACHE` 版本（当前 **v20**），否则抽旧壳
5. **WebView2 退出参数**：`tauri.conf.json` 中 `--disable-gpu-compositing --disable-gpu-rasterization --disable-backgrounding-occluded-windows --disable-renderer-backgrounding` 勿删（删除会复发随机退出）
6. **画布居中**：`#image` 的 `transform-origin` 必须 `50% 50%`
7. **马赛克互斥**：马赛克模式下文字拖拽互斥，先退马赛克再拖字
8. **磨皮/去噪/AI 放大已离线化**：内置 libheif + onnxruntime-web + sub_pixel_cnn 模型，不再联网；自定义模型地址在「设置→高级」自配
9. **格式限制**：HEIC 已支持离线解码；RAW/PSD 为扩展点；TIFF/TGA 丢 EXIF 方向；WebP 无 DPI 嵌入
10. **EXE 字节搜索不可用**：嵌入资源已压缩，验证只走 dist mtime + 实机截图
11. **内嵌 WebView2**：`bundle.windows.webviewInstallMode = "offlineInstaller"`（Setup.exe ~255MB）；Tauri v2 无 `bundledWebview2Runtime`（那是 v1 字段），须用 `webviewInstallMode`
12. **桌面 UIA 限制**：WebView2 无障碍树惰性加载，新实例 UIA 常查不到按钮；`MainWindowHandle` 可能指向隐形 helper 窗；后台 keybd_event/滚轮对 WebView2 无效。功能可靠性以 jsdom 单测为准，UIA 验收作为环境增强项
13. **jsdom 画布占位**：`<canvas>.getContext('2d')` 的 stub `getImageData/putImageData/createImageData` 存在但返回异常 → 任何像素函数**必须**对返回值判空（如瘦身 `slimCanvas` 的 `if (!src || !src.data)` / `if (!dst || !dst.data)`），否则未启用该功能时单测会踩到。预览类就地改像素用 `try/catch` 包裹
14. **面板形态**：美图 `#editMask`=**内嵌底栏** `.edit-panel`（`border-top`，不遮挡主图，符合硬约束）；批量 `#batchMask`=`.modal-mask` 遮罩弹窗。美图 5 分类 `.edit-tab`（美颜/风格配方/特效/高级/导出）+`.edit-pane`；批量 5 页签 `.batch-tab`

## 9. 当前状态与下一步

**已完成（可交付）**：桌面版安装包（NSIS+MSI）在 `src-tauri\target\release\bundle\`，已装本机；安装目录 `C:\Users\Administrator\AppData\Local\绿角犀看图\`；交付用 Setup.exe 已内嵌 WebView2 离线运行时并同步至项目根目录（`绿角犀看图_0.1.0_x64-setup.exe` 255.4MB / `绿角犀看图_0.1.0_x64_zh-CN.msi` 253.7MB，拷到 Win10/11 x64 双击即装即用、完全离线）。最新构建 **2026-09-10 14:20**（含瘦身/瘦脸）。

> **换机/交接提醒**：安装包与源码需一并带走；新机须装 Rust/VS Build Tools/WebView2 后，按第 3 节命令跑测试 + 按坑#1 构建。

**惯用迭代流程**（每次改前端都要走一遍）：改 `app.js`/`index.html` → `sw.js` 缓存版本 +1 → 跑 `npm test`（+ 相关 `test:xxx`）→ `node scripts/sync-dist.cjs`（或直接 `npm run build`，beforeBuildCommand 会自动 sync）→ 若 Rust 未变需 `cargo clean` 再 build → 安装产物同步到项目根 → 实机截图/UIA 验收。

**下一步候选（与用户确认优先级）**：
1. 鸿蒙真机验证（需 DevEco Studio 5.0+；本机工具链未装）
2. 体验美图/批量面板的实际效果并微调参数或 UI 细节
3. 功能体验细化（性能、反馈、边界）
4. 后续迭代：改前端 → 跑测试 → cargo clean + build → 安装验证 → UIA/实机截图
5. 桌面 UIA 环境已知惰性加载/隐形窗问题，验收优先以 jsdom 单测为准则