// 绿角犀看图·鸿蒙端 本地单板测试
import { describe, it, expect } from '@ohos/hypium';

export default function imageExtTest() {
  describe('imageExt', () => {
    it('Assert.dummy', 0, () => {
      expect(1).assertTrue();
    });
  });
}
